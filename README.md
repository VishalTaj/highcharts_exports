A Micro Service which accepts highcharts chart option data and return base64 png image.

## Start in Production mode

```
docker-compose up prod
```

## Start in development mode

```
docker-compose up dev
```
